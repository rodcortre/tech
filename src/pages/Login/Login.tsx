import styled, { css, keyframes, ThemeProvider } from "styled-components";
import { BackgroundContainer } from "../styled-components/Background";

const Login = () => {
  return (
    <BackgroundContainer>
      <div>Login</div>;
    </BackgroundContainer>
  );
};

export default Login;
